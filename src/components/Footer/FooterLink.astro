---
interface Props {
  href: string;
  newTab?: boolean;
  rest?: Partial<HTMLAnchorElement>;
}

const { href, newTab = false, ...rest } = Astro.props as Props;
---

<a
  href={href}
  class="mr-auto transition hover:text-primary-500"
  target={newTab ? "_blank" : undefined}
  rel={newTab ? "noopener noreferrer" : undefined}
  {...rest}
>
  <slot />
</a>
